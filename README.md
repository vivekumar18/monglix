# monglix
his program finds the length of the longest valid (well-formed) parentheses substring in a given string. It uses a stack to store the indices of opening parentheses and calculates the maximum length of valid matching parentheses while traversing the string. Finally, it displays the length of the longest valid parentheses substring.
